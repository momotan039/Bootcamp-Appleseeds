function getLength(arr){
    len=[]
    let i=0
    while(i<arr.length){
        len.push(arr[i++].length)
    }
    console.log(len)
    return len;
}
getLength(['adga','sd'])

//using loops depend on task
//if i'm gonna use complicated algorithm 
//I need to use while loop 'cause its allows
//me to controll on code
//but in this question i prefere to use for
